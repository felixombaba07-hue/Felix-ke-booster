import {useEffect,useMemo,useState} from 'react';import{Link,Route,Routes,useNavigate}from'react-router-dom';import{ArrowRight,CheckCircle,ChevronDown,Clock3,LayoutDashboard,LogIn,Menu,ShieldCheck,Sparkles,ShoppingCart,Users,X}from'lucide-react';import{supabase}from'./lib/supabase';import type{Service}from'./lib/types';
const fallback:Service[]=[
{id:'fb-followers',name:'Facebook Followers',category:'Followers',platform:'Facebook',price_per_1000:150,min_quantity:100,max_quantity:100000,active:true,description:'Facebook follower growth service.'},
{id:'ig-followers',name:'Instagram Followers',category:'Followers',platform:'Instagram',price_per_1000:350,min_quantity:100,max_quantity:100000,active:true,description:'Instagram follower growth service.'},
{id:'tt-followers',name:'TikTok Followers',category:'Followers',platform:'TikTok',price_per_1000:500,min_quantity:100,max_quantity:100000,active:true,description:'TikTok follower growth service.'},
{id:'x-followers',name:'X Followers',category:'Followers',platform:'X',price_per_1000:700,min_quantity:100,max_quantity:100000,active:true,description:'X follower growth service.'},
{id:'wa-followers',name:'WhatsApp Services',category:'Followers',platform:'WhatsApp',price_per_1000:650,min_quantity:100,max_quantity:100000,active:true,description:'WhatsApp growth service.'},
{id:'tg-subs',name:'Telegram Subscribers',category:'Subscribers',platform:'Telegram',price_per_1000:300,min_quantity:100,max_quantity:100000,active:true,description:'Telegram subscriber growth service.'},
{id:'fb-likes',name:'Facebook Likes',category:'Likes',platform:'Facebook',price_per_1000:100,min_quantity:100,max_quantity:100000,active:true,description:'Facebook likes service.'},
{id:'ig-likes',name:'Instagram Likes',category:'Likes',platform:'Instagram',price_per_1000:150,min_quantity:100,max_quantity:100000,active:true,description:'Instagram likes service.'},
{id:'tt-likes',name:'TikTok Likes',category:'Likes',platform:'TikTok',price_per_1000:80,min_quantity:100,max_quantity:100000,active:true,description:'TikTok likes service.'}
];
function Layout({children}:{children:React.ReactNode}){const[open,setOpen]=useState(false);return <div className="min-h-screen"><header className="sticky top-0 z-50 border-b border-white/10 bg-[#050914]/85 backdrop-blur-xl"><div className="mx-auto flex max-w-6xl items-center justify-between px-4 py-4"><Link to="/" className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-gradient-to-br from-cyan-300 to-blue-600 font-black text-slate-950">F</div><div><div className="font-black tracking-wide">FELIX KE BOOSTER PRO</div><div className="text-xs text-slate-400">Your Digital Growth Starts Here.</div></div></Link><button className="md:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button><nav className={`${open?'flex':'hidden'} absolute left-0 right-0 top-full flex-col gap-2 border-b border-white/10 bg-[#050914] p-4 md:static md:flex md:flex-row md:border-0 md:bg-transparent md:p-0`}><Link className="btn btn-ghost" to="/services">Services</Link><Link className="btn btn-ghost" to="/dashboard">Dashboard</Link><Link className="btn btn-primary flex items-center gap-2" to="/login"><LogIn size={17}/>Login</Link></nav></div></header><main>{children}</main><footer className="border-t border-white/10 py-10 text-center text-sm text-slate-500">© {new Date().getFullYear()} FELIX KE BOOSTER PRO. Built for Kenya.</footer></div>}
function Home(){return <Layout><section className="mx-auto max-w-6xl px-4 pb-20 pt-20"><div className="grid items-center gap-12 md:grid-cols-2"><div><div className="mb-4 inline-flex items-center gap-2 rounded-full border border-cyan-400/20 bg-cyan-400/5 px-3 py-2 text-xs text-cyan-200"><Sparkles size={14}/> Fast digital growth services</div><h1 className="text-5xl font-black leading-tight md:text-7xl">Grow your <span className="neon">digital presence.</span></h1><p className="mt-6 max-w-xl text-lg leading-8 text-slate-400">Order social-media growth services, track delivery, and manage everything from one clean dashboard.</p><div className="mt-8 flex flex-wrap gap-3"><Link className="btn btn-primary inline-flex items-center gap-2" to="/services">Explore Services <ArrowRight size={18}/></Link><Link className="btn btn-ghost" to="/login">Create Account</Link></div><div className="mt-8 grid max-w-lg grid-cols-3 gap-3 text-sm"><Stat icon={<Clock3/>} text="Fast delivery"/><Stat icon={<ShieldCheck/>} text="Secure checkout"/><Stat icon={<Users/>} text="24/7 dashboard"/></div></div><div className="glass rounded-3xl p-5"><div className="mb-5 flex items-center justify-between"><div><div className="text-sm text-slate-400">Live marketplace</div><div className="text-xl font-bold">Popular services</div></div><ShoppingCart className="text-cyan-300"/></div><div className="space-y-3">{fallback.slice(0,5).map(s=><div key={s.id} className="flex items-center justify-between rounded-2xl border border-white/5 bg-white/[.03] p-4"><div><div className="font-semibold">{s.name}</div><div className="text-xs text-slate-500">per 1,000</div></div><div className="font-black text-cyan-300">KSh {s.price_per_1000}</div></div>)}</div></div></div></section></Layout>}
function Stat({icon,text}:{icon:React.ReactNode;text:string}){return <div className="glass rounded-2xl p-3 text-slate-300"><div className="mb-2 text-cyan-300">{icon}</div>{text}</div>}
function Services(){const[services,setServices]=useState< Service[]>(fallback);const[loading,setLoading]=useState(false);useEffect(()=>{(async()=>{if(!supabase)return;setLoading(true);const{data}=await supabase.from('services').select('*').eq('active',true).order('created_at',{ascending:false});if(data?.length)setServices(data as Service[]);setLoading(false)})();},[]);return <Layout><section className="mx-auto max-w-6xl px-4 py-14"><div className="mb-8"><div className="text-sm text-cyan-300">Marketplace</div><h2 className="mt-2 text-4xl font-black">Services</h2><p className="mt-2 text-slate-400">Choose a service, enter your target and place an order.</p></div>{loading&&<p className="mb-4 text-slate-400">Loading live services…</p>}<div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{services.map(s=><ServiceCard key={s.id} service={s}/>)}</div></section></Layout>}
function ServiceCard({service:s}:{service:Service}){const nav=useNavigate();const[qty,setQty]=useState(100);const total=(qty/1000)*s.price_per_1000;return <div className="glass rounded-3xl p-5"><div className="mb-4 flex items-start justify-between"><div><div className="text-xs uppercase tracking-wider text-cyan-300">{s.platform} · {s.category}</div><h3 className="mt-1 text-xl font-bold">{s.name}</h3></div><span className="rounded-full bg-cyan-400/10 px-2 py-1 text-xs text-cyan-200">KSh {s.price_per_1000}/1K</span></div><p className="mb-5 text-sm text-slate-400">{s.description}</p><label className="text-xs text-slate-500">Quantity</label><input className="field mt-1" type="number" min={s.min_quantity} max={s.max_quantity} value={qty} onChange={e=>setQty(Number(e.target.value)||0)}/><div className="mt-4 flex items-center justify-between"><span className="text-slate-400">Total</span><b className="text-xl text-cyan-300">KSh {total.toFixed(2)}</b></div><button className="btn btn-primary mt-4 w-full" onClick={()=>nav('/login',{state:{serviceId:s.id,quantity:qty}})}>Order now</button></div>}
function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[signup,setSignup]=useState(false);const[msg,setMsg]=useState('');const nav=useNavigate();async function submit(e:React.FormEvent){e.preventDefault();setMsg('');if(!supabase){setMsg('Add Supabase environment variables first.');return}const res=signup?await supabase.auth.signUp({email,password}):await supabase.auth.signInWithPassword({email,password});if(res.error)setMsg(res.error.message);else{setMsg(signup?'Account created. Check your email if confirmation is enabled.':'Signed in.');setTimeout(()=>nav('/dashboard'),500)}}return <Layout><section className="mx-auto max-w-md px-4 py-16"><div className="glass rounded-3xl p-7"><h2 className="text-3xl font-black">{signup?'Create account':'Welcome back'}</h2><p className="mt-2 text-slate-400">{signup?'Start managing your digital growth.':'Sign in to your dashboard.'}</p><form onSubmit={submit} className="mt-7 space-y-4"><input className="field" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="field" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} minLength={6} required/><button className="btn btn-primary w-full">{signup?'Create account':'Sign in'}</button></form>{msg&&<p className="mt-4 text-sm text-cyan-200">{msg}</p>}<button className="mt-5 w-full text-sm text-slate-400" onClick={()=>setSignup(!signup)}>{signup?'Already have an account? Sign in':'New here? Create an account'}</button></div></section></Layout>}
function Dashboard(){const[user,setUser]=useState<any>(null);const[orders,setOrders]=useState<any[]>([]);useEffect(()=>{(async()=>{if(!supabase)return;const{data:{user}}=await supabase.auth.getUser();setUser(user);if(user){const{data}=await supabase.from('orders').select('id,service_name,quantity,total_amount,status,created_at').order('created_at',{ascending:false});setOrders(data||[])}})()},[]);if(!supabase)return <Layout><Empty text="Connect Supabase to use the dashboard."/></Layout>;return <Layout><section className="mx-auto max-w-6xl px-4 py-12"><div className="mb-8 flex flex-col justify-between gap-4 md:flex-row md:items-center"><div><div className="text-cyan-300">Dashboard</div><h2 className="text-4xl font-black">Welcome back</h2><p className="text-slate-400">{user?.email||'Please sign in to view your account.'}</p></div><Link className="btn btn-primary" to="/services">New order</Link></div><div className="grid gap-4 md:grid-cols-3"><Stat icon={<ShoppingCart/>} text={`${orders.length} orders`}/><Stat icon={<CheckCircle/>} text="Secure payments"/><Stat icon={<LayoutDashboard/>} text="Live tracking"/></div><div className="glass mt-6 rounded-3xl p-5"><h3 className="mb-4 text-xl font-bold">Recent orders</h3>{orders.length===0?<p className="text-slate-400">No orders yet.</p>:<div className="space-y-3">{orders.map(o=><div key={o.id} className="flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-white/5 bg-white/[.03] p-4"><div><b>{o.service_name}</b><div className="text-xs text-slate-500">Qty {o.quantity}</div></div><div className="text-right"><div className="text-cyan-300">KSh {Number(o.total_amount).toFixed(2)}</div><div className="text-xs text-slate-500">{o.status}</div></div></div>)}</div>}</div></section></Layout>}
function Empty({text}:{text:string}){return <section className="mx-auto max-w-3xl px-4 py-24 text-center"><div className="glass rounded-3xl p-10"><h2 className="text-3xl font-black">Setup required</h2><p className="mt-3 text-slate-400">{text}</p></div></section>}
export default function App(){return <Routes><Route path="/" element={<Home/>}/><Route path="/services" element={<Services/>}/><Route path="/login" element={<Login/>}/><Route path="/dashboard" element={<Dashboard/>}/><Route path="*" element={<Home/>}/></Routes>}
